export function globalConfig () {
    const globalConfig = {
        starsLevel: [1, 2, 3, 4, 5], // 设置评价 星星等级
        commentList: [], // 评论
    }
    return globalConfig;
}

export default globalConfig();